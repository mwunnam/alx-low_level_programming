#include "main.h"
#include <stdio.h>

/**
 * swap_intc(int *a, int *b)
 *
 * @a: is the first parameter
 * @b: is the second parameter
 *
 * Return: Always 0.
 * */

void swap_int(int *a, int *b)
{
	*a = 42;
	*b = 98;
}
