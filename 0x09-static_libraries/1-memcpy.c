#include <stdio.h>
#include "main.h"

/**
* _memset - Entry point
*
* @s: input
* @b: input
* @n: input
*
* Return: 0 (success)
*/

char *_memset(char *s, char b, unsigned int n)
{
	/* empty function */
return (0);
}
