#include <stdio.h>
#include <stdio.h>

/**
* op_add - Funcation that calculate the sum
* @a: number 1
* @b: number 2
* Return: sun of a and b
*/

int op_add(int a, int b)
{
return (a + b);
}

/**
* op_sub - Funcation that calculate the sum
* @a: number 1
* @b: number 2
* Return: difference of a and b
*/

int op_sub(int a, int b)
{
return (a - b);
}


/**
* op_mul - Funcation that calculate the sum
* @a: number 1
* @b: number 2
* Return: product of a and b
*/
int op_mul(int a, int b)
{
return (a * b);
}

/**
* op_div - Funcation that calculate the sum
* @a: number 1
* @b: number 2
* Return: division of a and b
*/
int op_div(int a, int b)
{
	if (b == 0)
		return

return (a / b);
}

/**
* op_mod - Funcation that calculate the sum
* @a: number 1
* @b: number 2
* Return: division of a and b
*/
int op_mod(int a, int b)
{
return (a % b);
}

