#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{	char *message = "Programming is like building a multilingual puzzle\n";
	puts(message);
	return (0);
}
