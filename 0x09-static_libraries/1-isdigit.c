#include "main.h"

/**
 * _isdigit - function that print on the standard output
 *
 * @c: parameter to be entered
 *
 * Return: 0 when successsfull
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
